import 'package:flutter/material.dart';

class CustomIconStar extends StatelessWidget {
  const CustomIconStar({super.key});

  @override
  Widget build(BuildContext context) {
    return Icon(Icons.star, size: 18, color: Colors.amber);
  }
}
